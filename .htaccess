RewriteEngine On

# Force all traffic into the public/ subdirectory.
# This handles the case where the virtual-host / alias docroot is set
# to the project root instead of project/public.

RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]
